<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@android:color/transparent">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <ImageView
            android:id="@+id/arrow_back"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_centerVertical="true"
            android:src="@drawable/baseline_arrow_back_24"
            app:tint="?attr/colorControlNormal" />

        <ImageView
            android:id="@+id/buttonPinToggle"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_centerVertical="true"
            android:layout_toStartOf="@id/trash_bin"
            android:layout_marginEnd="16dp"
            android:src="@drawable/keep_outline"
            android:clickable="true"
            android:focusable="true"
            app:tint="?attr/colorControlNormal" />

        <ImageView
            android:id="@+id/trash_bin"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_centerVertical="true"
            android:layout_alignParentEnd="true"
            android:src="@drawable/outline_auto_delete_24"
            app:tint="?attr/colorControlNormal" />

        <!-- TODO: Add buttons for pinned, reminder-->

    </RelativeLayout>

</RelativeLayout>
